[package]
name = "postgres"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
sqlx = "0.6.2"
orca-core = { registry = "orcalabs-orcastrator", version = "0.19.0", features = ["telemetry"] }

